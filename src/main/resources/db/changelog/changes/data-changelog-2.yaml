databaseChangeLog:
  - changeSet:
      id: 5
      author: admin
      changes:
        - sql:
            sql: UPDATE `user` SET `account_non_expired` = true, `account_non_locked` = true, `credentials_non_expired` = true, `enabled` = true;